kill(all)$
load("vect")$
y:x^2$
F:[y,-x]$
dx:diff(x,x)$
dy:diff(y,x)$
dr:[dx,dy]$
I:(F.dr)$
integrate(I,x,0,1);

kill(all)$
load("vect")$
x:3*t+1$
y:t+1$
F:[x+y,y-x]$
dx:diff(x,t)$
dy:diff(y,t)$
dt:[dx,dy]$
I:(F.dt)$
integrate(I,t,0,1);
